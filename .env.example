# 环境变量配置
# 复制此文件为 .env.local 并按需修改

# 网关地址（留空使用默认）
LIGHTWHEEL_BASE_URL=

# 数据库路径（相对于项目根目录）
DATABASE_PATH=data/app.db

# TLS 证书验证（默认开启，自签名网关需设为 false）
LIGHTWHEEL_TLS_VERIFY=true

# 生成超时（秒）
GENERATION_TIMEOUT=300

# 图片输出目录（相对于项目根目录）
OUTPUTS_DIR=public/outputs